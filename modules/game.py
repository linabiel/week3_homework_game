class Game:
    def __init__(self, name, player):
        self.name = name
        self.player = player
        winner = []

def compare_choices(self, player1, player2):
    self.player

    if player1.choice == "Rock" and player2.choice == "Scissors":
        self.player.name = player1
    return self.winner.append(self.player.name)
    # if self.player.choice == "Rock" and self.player.choice == "Paper":
    #     return self.player.name
    # if self.player.choice == "Paper" and self.player.choice == "Scissors":
    #     return self.player.name

